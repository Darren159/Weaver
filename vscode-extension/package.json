{
  "name": "weaver",
  "displayName": "Weaver",
  "description": "Surfaces related docs as you code + inline completions, powered by Elasticsearch hybrid search (BM25 + ELSER) and LLM",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.110.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "weaver",
          "title": "Weaver",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "weaver": [
        {
          "type": "webview",
          "id": "weaver.docsView",
          "name": "Related Docs"
        }
      ]
    },
    "commands": [
      {
        "command": "weaver.openChat",
        "title": "Weaver: Open Chat",
        "icon": "$(comment-discussion)"
      }
    ],
    "keybindings": [
      {
        "command": "weaver.openChat",
        "key": "ctrl+shift+k",
        "mac": "cmd+shift+k"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "weaver.openChat",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Weaver",
      "properties": {
        "weaver.backendUrl": {
          "type": "string",
          "default": "http://localhost:8000",
          "description": "URL of the Weaver backend API"
        },
        "weaver.debounceMs": {
          "type": "number",
          "default": 500,
          "description": "Debounce delay (ms) before triggering a doc search on cursor move"
        },
        "weaver.bridgePort": {
          "type": "number",
          "default": 8765,
          "description": "Port for the Weaver desktop panel bridge server (used to apply code edits from the floating panel)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.110.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.56.1",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
